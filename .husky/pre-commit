#!/bin/sh

# Type check
pnpm check:type

# Auto-bump version (default: patch, override with BUMP=minor/major)
node scripts/bump-version.js

# Stage version files
git add package.json src-tauri/tauri.conf.json
